%h1.logo= link_to 'MOKU', root_path
.user
  - if current_user
    logged in as 
    #{link_to current_user.name, current_user} | #{link_to 'Log out', destroy_user_session_path, method: :delete}
  - else
    #{link_to 'Log in', new_user_session_path} | #{link_to "Sign up", new_user_registration_path}
.clearfix